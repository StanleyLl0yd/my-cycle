version: "1.0"

linter: jetbrains/qodana-jvm-community:2026.2

profile:
  name: qodana.recommended

projectJDK: "21"
failThreshold: 0

exclude:
  - name: All
    paths:
      - ".gradle"
      - ".gradle-user-home"
      - "build"
      - "app/build"
      - ".idea"
      - ".qodana"
      - ".qodana-cache"
