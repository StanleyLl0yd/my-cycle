# RuStore visual assets status — 1.4.0

Text package and visual-generation workflow are prepared in PR #58.

## Verified store visuals

The final phone screenshot set was generated by the `RuStore Screenshots` workflow from real My Cycle 1.4.0 UI and passed the repository validator:

- `01-today.png` — explainable approximate forecast;
- `02-calendar.png` — calendar history and predicted range;
- `03-statistics.png` — recent cycle statistics;
- `04-insights.png` — recurring symptom patterns;
- `05-privacy-report.png` — privacy controls and local PDF report.

The verified CI artifact was produced from commit `fc12ec8e6bb3ba299e19530ee682c6743ff256d1` by workflow run `34810036568`. Changes after that commit are store-documentation-only and do not alter application code, demo data, screenshot capture logic, or the screenshot workflow.

The screenshots are portrait 1080×1920, contain no Android System UI, and use deterministic debug-only demo data. They are generated as CI artifacts rather than committed as binary files.

## External package assets

The final external package should additionally contain:

- `icon-512.png` derived by resize from the approved application artwork;
- the five raw CI screenshots above;
- five ASO compositions using the same real UI and truthful Russian overlay text;
- optional promo composition for external marketing only.

The signed AAB/APK are not regenerated for store packaging; use the immutable GitHub Release `v1.4.0`.
