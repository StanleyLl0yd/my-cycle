# RuStore visual assets status — 1.4.0

Text package is prepared in PR #58.

Pending visual deliverables before the final external ZIP can be considered complete:

- 512×512 store icon matching the installed app icon;
- 4–5 portrait 9:16 screenshots reflecting the actual 1.4.0 UI;
- optional promo composition if needed for external marketing, not required by the base RuStore mobile listing.

The repository already contains a `store-screenshots.yml` workflow intended to generate phone screenshots. The next step is to run or update that workflow so that it captures the 1.4.0 Today, Calendar, Statistics/Insights, Diary and Settings/PDF screens described in `screenshot-plan.ru.md`.
